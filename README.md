# 💝 DonationApp

Современное приложение для приема пожертвований на Vue 3 с полной типизацией и валидацией форм.

## ✨ Возможности

- 📝 Умная форма пожертвования с пошаговой валидацией
- 🌍 Автоматическая маска телефона для разных стран
- 💰 Форматирование сумм в реальном времени
- 🎨 Темная и светлая тема
- 📱 Адаптивный дизайн
- ✅ Строгая валидация (email, телефон, сумма, возраст)

## 🛠 Технологии

**Основной стек:**

- Vue 3 (Composition API + `<script setup>`)
- TypeScript
- Vite

**UI и стили:**

- Tailwind CSS 4 + @tailwindcss/vite
- reka-ui (shadcn-vue компоненты)
- lucide-vue-next + iconify-f7 (иконки)
- @formkit/auto-animate (анимации)

**Формы и валидация:**

- zod (валидация форм)
- libphonenumber-js (валидация телефонов)
- vue-currency-input (ввод сумм)

**Роутинг и состояние:**

- Vue Router 4
- Pinia

**Тестирование:**

- Playwright

## 🚀 Быстрый старт

```bash
# Установка зависимостей
pnpm install

# Запуск dev-сервера
pnpm dev
```

Приложение откроется на `http://localhost:5173`

## 📦 Команды

### Разработка

```bash
pnpm dev          # запуск dev-сервера
pnpm build        # production сборка
pnpm preview      # предпросмотр production сборки
pnpm sa           # добавить shadcn-vue компонент
```

### Тестирование

```bash
pnpm test         # запуск E2E тестов
pnpm test:ui      # запуск тестов в UI режиме
pnpm test:debug   # запуск тестов в debug режиме
pnpm test:report  # показать отчет о тестах
```

## 📁 Структура проекта

```
src/
├── components/       # UI компоненты
│   ├── ui/          # shadcn-vue компоненты
│   ├── donation/    # компоненты формы пожертвований
│   └── common/      # общие компоненты (Header, Footer)
├── pages/           # страницы приложения
├── stores/          # Pinia хранилища
├── router/          # настройка Vue Router
├── composables/     # переиспользуемая логика
├── lib/             # утилиты, константы, типы, валидация
└── styles/          # глобальные стили
```

---

<p align="center">Сделано с ❤️ на Vue 3 + TypeScript</p>
